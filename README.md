# Beeline

Campus wayfinding for Georgia Tech, in Swift 6 and SwiftUI.

Search a room, a course, or a place; get walking directions to the right *door*; see which floor and wing the room is on; and watch the Stinger buses actually move. Works entirely offline — the campus data ships inside the app.

## Why

GT's official map stops at the building outline, and the bus app shows a route map and little else. Nothing tells a first-year that Klaus 1443 and Klaus 2443 are on different floors, or that the Clough entrance nearest the bus stop puts you on the second floor. Beeline is the last 200 meters.

## What it does

- **Search anything** — `Klaus 1443`, `CS 1332`, "Clough", "food", "restrooms". One field, ranked results across 380 buildings, 333 rooms and 382 places.
- **Find a free study room** — all 41 bookable library rooms with *live* availability from LibCal: which are free, until when, and one swipe to book. Free rooms sort first.
- **Walk you there** — A* over a 12,400-node campus path graph built from OpenStreetMap footways. It picks the closest usable entrance, prefers paths over roads, and can avoid stairs entirely.
- **Tell you where the room is** — for Clough, the actual published floor plan with your room ringed on it and a floor picker down the side. Everywhere else, floor and wing decoded from the room number, with per-building rules for the ones that break convention (Howey's `L3` lecture halls, Klaus's atrium halls, College of Computing's two-digit rooms). The app says which of the two it's showing, because the confidence differs.
- **Know your schedule** — add your sections and the map opens on "next class, 6 min walk, leave by 1:24".
- **Show the buses** — all nine Stinger routes with live vehicle positions and per-stop arrival estimates on the same screen.
- **Walk or ride?** — for any destination it plans walk-bus-walk trips against live arrivals and offers the bus *only* when it beats walking by a real margin and you can actually catch it. This is the thing the bus app can't do, because it doesn't know where you're going.

## Architecture

```
Sources/
├── BeelineCore/          Pure Swift. No SwiftUI, no MapKit.
│   ├── Models/           The campus data pack, decoded
│   ├── Routing/          A* router, turn-by-turn directions, walk/bus trip planning
│   ├── Bus/              Ride Systems live client (+ a preview double)
│   ├── Indoor/           Traced floor graphs: cross-floor routing and directions
│   ├── Study/            LibCal availability: free-now, free-until
│   ├── RoomDecoder       Room number → floor, wing, hint
│   ├── FloorPlan         Published plans: page image + room positions
│   ├── Search            One index over buildings, rooms, courses, places
│   └── Schedule          Enrolled sections → weekly events, "what's next"
├── BeelineUI/            SwiftUI views and the @Observable AppModel
└── App/Beeline/          @main — ten lines
Tests/BeelineCoreTests/   83 XCTest cases
```

The core has no UI dependency, so it builds and tests on a Mac with `swift test` — no simulator needed. `BeelineUI` compiles for macOS too, which keeps the whole package type-checkable from the command line.

**The data pack** (`Sources/BeelineCore/Resources/campus.json`, ~2.8 MB) is generated by [beeline-api](https://github.com/sid-dutta/beeline-api) from OpenStreetMap, Georgia Tech's Concept3D campus map, the registrar's schedule of classes, and the Stinger bus feed. Routing runs on the phone, so the only thing that needs the network is live bus positions.

**Routing.** Edge cost is meters × a multiplier by kind, so a footway beats a service road beats a main road, and steps are flagged so step-free routing can exclude them. Buildings expose every mapped entrance; the router solves many-to-many across them and reports which door it chose.

## Design

Colours adapt: the navy accent lifts on dark grounds so it still reads as tappable, map chrome uses system materials, and pin bodies follow the system background the way Apple Maps' own annotations do. Route colours are the exception — the Red route is red in either theme, because that is its identity. Both themes were checked on device.

## Building

Requires Xcode 16 or later.

```bash
swift build && swift test     # core + UI, no simulator

brew install xcodegen         # only if you edit project.yml
xcodegen generate
open Beeline.xcodeproj
```

## Status

Working: search, outdoor routing with turn-by-turn, room decoding, Clough floor plans, class schedule, live buses, live study-room availability.

**On place data.** Georgia Tech publishes points for *gender-inclusive single-occupancy* restrooms (82 of them) — not for every restroom — so that is exactly what the app calls them. The 54 vending machines are all named "Vending Machine" upstream, so the pipeline appends the building each one sits in. Food comes mostly from OpenStreetMap, which has 5× the coverage of the campus map. Bus stops come from the bus feed itself.

Clough is the only building at Georgia Tech that publishes floor plans. Its five pages are parsed for room positions in the pipeline and shipped as images — 81% of Clough's scheduled class meetings land on an exact spot on a real plan; the rest still get the right floor.

Indoor routing is built and tested against fixtures, ready for the first traced building: it routes entrance → corridor → stairs or lift → room across floors, refuses stairs when you ask for step-free, and turns the path into sentences with real left/right taken from the traced geometry. It reports **no indoor distances or times** — traced positions have no real-world scale, so a "40 ft" would be invented.

Graphs are authored with the tracer in [beeline-api](https://github.com/sid-dutta/beeline-api); there is no indoor positioning at Georgia Tech, so guidance is instructions ("take the stairs up to the 2nd floor, room 2443 is on your left"), never a moving dot.

## License

MIT. Map data © OpenStreetMap contributors (ODbL).
